# Requirements para o Assistente de Voz Chica
# Python 3.8+ (testado com Python 3.12.4)
# Instale com: pip install -r requirements.txt

# ============================================================================
# RECONHECIMENTO DE VOZ (STT)
# ============================================================================
faster-whisper>=1.0.0          # STT via CTranslate2 — 4x mais rápido que whisper original
                                # No Mac usa whisper original (GPU/MPS), no SBC usa este (CPU int8)

# ============================================================================
# SÍNTESE DE VOZ (TTS)
# ============================================================================
kokoro>=0.1.0                  # TTS local (padrão) — português (pf_dora), inglês, etc.
edge-tts>=6.0.0                # TTS online via Microsoft Edge — voz Thalita (pt-BR)
# qwen3-tts>=0.1.0             # TTS Qwen3 (opcional — instale se for usar)

# ============================================================================
# CLIENTE LLM (Ollama / LM Studio / llama.cpp)
# ============================================================================
ollama>=0.3.0                  # Para LLM local via Ollama
openai>=1.0.0                  # Para LM Studio / llama.cpp (API compatível com OpenAI)
requests>=2.31.0               # Fallback para LLM + web_search

# ============================================================================
# PROCESSAMENTO DE ÁUDIO
# ============================================================================
soundfile>=0.12.0              # Leitura/escrita de arquivos de áudio (.wav)
sounddevice>=0.4.6             # Captura e reprodução de áudio em tempo real
numpy>=1.24.0,<2.0             # Processamento numérico (compatível com ML libs)

# ============================================================================
# INTERFACE GRÁFICA (AVATAR)
# ============================================================================
pygame>=2.5.0                  # Avatar animado (opcional — desabilite com AVATAR_ENABLE=False)

# ============================================================================
# FRAMEWORKS DE DEEP LEARNING
# ============================================================================
torch>=2.2.0                   # PyTorch — necessário para Kokoro e whisper original
                                # Python 3.12 requer torch>=2.2.0

# ============================================================================
# UTILITÁRIOS
# ============================================================================
colorama>=0.4.6                # Cores no terminal
tqdm>=4.66.0                   # Barras de progresso

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
# 1. OLLAMA: Deve ser instalado separadamente (https://ollama.ai/)
# 2. LLAMA.CPP: Compile ou instale (https://github.com/ggml-org/llama.cpp)
# 3. MODELOS: Baixados automaticamente na primeira execução
# 4. GPU NVIDIA: pip install torch --index-url https://download.pytorch.org/whl/cu118
# 5. APPLE SILICON (MPS): torch já vem com suporte MPS no macOS
# 6. RPi / ARM: torch CPU é suficiente (faster-whisper usa CTranslate2, não torch)

# ============================================================================
# DEPENDÊNCIAS DE SISTEMA (instalar antes do pip)
# ============================================================================
# macOS:
#   brew install sdl2 sdl2_image sdl2_mixer sdl2_ttf portaudio sox
#
# Ubuntu/Debian (incluindo Raspberry Pi OS):
#   sudo apt install python3-dev libsdl2-dev libsdl2-image-dev \
#     libsdl2-mixer-dev libsdl2-ttf-dev portaudio19-dev sox \
#     pulseaudio pulseaudio-module-bluetooth
#
# Windows (para Pygame):
#   Baixe do site oficial do Pygame ou use: pip install pygame
